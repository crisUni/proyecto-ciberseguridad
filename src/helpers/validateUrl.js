function validateUrl(url) {
  // TODO: implement URL validation
  // This file should ONLY be modified on the secure branch
  // Vulnerable branch must leave this as a pass-through
  return url;
}

module.exports = { validateUrl };
